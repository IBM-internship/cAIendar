services:
  dotnet-dev:
    build: .
    image: my/dotnet9-dev
    volumes:
      - ./:/workspace
    working_dir: /workspace
    ports:
      - "2223:22"
    extra_hosts:
      - "host.docker.internal:host-gateway"

